<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
        }
        .container {
            max-width: 500px;
            margin: auto;
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .success_img {
            width: 100px;
            margin: 20px 0;
        }
        .green_btn {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .green_btn:hover {
            background-color: #45a049;
        }
      </style>
</head>
<body>
    <div class="container">
        <form class="form_container" method="POST" action="/password-reset">
            <h1>Forgot Password</h1>
            <input
                type="email"
                placeholder="Email"
                name="email"
                required
                class="input"
            />
            <% if (typeof error !== 'undefined') { %>
                <div class="error_msg"><%= error %></div>
            <% } %>
            <% if (typeof msg !== 'undefined') { %>
                <div class="success_msg"><%= msg %></div>
            <% } %>
            <button type="submit" class="green_btn">
                Submit
            </button>
        </form>
    </div>
    <script>
        // You can add client-side JavaScript here if needed
    </script>
</body>
</html>
