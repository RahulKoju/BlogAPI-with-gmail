<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
        }
        .container {
            max-width: 500px;
            margin: auto;
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .success_img {
            width: 100px;
            margin: 20px 0;
        }
        .green_btn {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .green_btn:hover {
            background-color: #45a049;
        }
      </style>
</head>
<body>
    <div class="container">
        <% if (validUrl) { %>
            <form class="form_container" method="POST" action="<%= url %>">
                <h1>Add New Password</h1>
                <input
                    type="password"
                    placeholder="Password"
                    name="password"
                    required
                    class="input"
                />
                <% if (error) { %>
                    <div class="error_msg"><%= error %></div>
                <% } %>
                <% if (msg) { %>
                    <div class="success_msg"><%= msg %></div>
                <% } %>
                <button type="submit" class="green_btn">
                    Submit
                </button>
            </form>
        <% } else { %>
            <h1>404 Not Found</h1>
        <% } %>
    </div>
    <script>
        // Additional JavaScript logic if needed
    </script>
</body>
</html>
